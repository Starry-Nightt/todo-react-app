export enum Theme {
  LIGHT = 1,
  DARK,
}

export enum TodoStatus {
  NEW = 1,
  IN_PROGRESS,
  COMPLETED,
}

export enum TodoPriority {
  LOW = 1,
  NORMAL,
  HIGH,
}

export enum LocalStorageKey {
  THEME = "theme",
}
